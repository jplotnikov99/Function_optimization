C++ code to find good coefficients for a pade approximation of non-analyitcal functions.
### Build

You can build the program with 

        cd path/to/RelExt
        mkdir build && cd build  
        cmake ..  
        make 
